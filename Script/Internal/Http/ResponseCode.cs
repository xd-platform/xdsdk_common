#if UNITY_EDITOR || UNITY_STANDALONE
namespace XD.SDK.Common.PC.Internal {
    public static class ResponseCode {
        // Token 过期
        public static readonly int TOKEN_EXPIRED = 40801;
    }
}
#endif